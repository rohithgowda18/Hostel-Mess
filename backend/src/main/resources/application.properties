# Application Name
spring.application.name=mess-breakfast

# Server Port
server.port=${PORT:8080}

# MongoDB Configuration (uses environment variable with fallback)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://rohithgowdak18:Rohk_1265@cluster0.bqf9l9q.mongodb.net/hostel_mess?retryWrites=true&w=majority}

# JWT Configuration - Use a fixed secret for development
jwt.secret=your-secret-key-for-development-with-32-or-more-characters-minimum
jwt.expiration=${JWT_EXPIRATION:86400000}

# Logging (useful for debugging)
logging.level.org.springframework.data.mongodb=INFO
logging.level.com.hostel.mess=DEBUG
