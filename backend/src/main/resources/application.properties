# ==========================================
# Application Configuration
# ==========================================
spring.application.name=mess-breakfast
server.port=${PORT:8080}

# ==========================================
# Database Configuration
# ==========================================
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:${MONGODB_URI:mongodb+srv://rohithgowdak18:Rohk_1265@cluster0.bqf9l9q.mongodb.net/hostel_mess?retryWrites=true&w=majority&serverSelectionTimeoutMS=30000&socketTimeoutMS=120000&connectTimeoutMS=15000&readPreference=secondaryPreferred&heartbeatFrequencyMS=5000}}
spring.data.mongodb.auto-index-creation=true

# ==========================================
# Security & JWT Configurations
# ==========================================
jwt.secret=${JWT_SECRET:your-secret-key-for-development-with-32-or-more-characters-minimum}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ==========================================
# Multipart Upload Settings
# ==========================================
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# ==========================================
# Logging Configurations
# ==========================================
logging.level.root=INFO
logging.level.org.springframework.data.mongodb=WARN
logging.level.org.mongodb=WARN
logging.level.com.hostel.mess=DEBUG
