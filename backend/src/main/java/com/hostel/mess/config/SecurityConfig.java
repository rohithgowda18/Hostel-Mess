package com.hostel.mess.config;

/**
 * Spring Security configuration - Authentication disabled
 * All endpoints are now public
 */
public class SecurityConfig {
    // Security config disabled - all endpoints are public
}
