package com.hostel.mess.security;

/**
 * JWT Authentication Filter - Disabled
 * Authentication is disabled so this filter is not used
 */
public class JwtAuthenticationFilter {
    // JWT authentication disabled
}
